const { addLanguage } = require('./mutation')
const { Language1 } = require('./object')
const { usersQueryField } = require('./query')

module.exports={
    addLanguage,
    Language1,usersQueryField
}